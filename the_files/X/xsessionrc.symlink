# -*- mode: shell-script -*-
# ~/.xsessionrc
#
# Sourced by X session scripts on Debian and Ubuntu.

[ -r /etc/profile ] && . /etc/profile
[ -r ~/.profile ] && . ~/.profile

case "$-" in
    *i*) if [ -r ~/.bashrc ]; then
	     . ~/.bashrc;
	 fi
	 ;;
esac

set-lang -l 5
