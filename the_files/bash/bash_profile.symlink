# -*-shell-script-*-
# ~/.bash_profile: executed by Bash login shell.

if [ -r ~/.profile ]; then
    . ~/.profile;
fi

# export SHELL=${SHELL:-${BASH:-/bin/bash}}

case "$-" in
    *i*) if [ -r ~/.bashrc ]; then
	     . ~/.bashrc;
	 fi
	 ;;
esac

